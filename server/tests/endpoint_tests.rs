//! Endpoint tests runner
//!
//! This module includes all endpoint test submodules and makes them discoverable as a single test binary by Cargo.

mod common;
mod endpoint;

// TODO: Re-export all endpoint test modules so they can be run
// pub use endpoint::*;
